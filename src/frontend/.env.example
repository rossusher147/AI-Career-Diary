VITE_API_BASE_URL=http://localhost:8000
VITE_KEYCLOAK_URL=http://localhost:8080
VITE_KEYCLOAK_REALM=aicd
VITE_KEYCLOAK_CLIENT_ID=aicd-web

# Development-only helper for UI checks when Keycloak is not running.
# This is ignored by production builds.
# VITE_AUTH_MODE=mock
# VITE_API_MODE=mock
